# No API key needed — generation runs through your local, already-authenticated
# `claude` CLI (Claude Code), using your existing subscription/login.
# Copy this file to .env only if you want to override a default below.

# PORT=4173
# CLAUDE_MODEL=sonnet
